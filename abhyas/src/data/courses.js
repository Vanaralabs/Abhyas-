// Course data hub — unifies all course data and provides helper functions
import { htmlCourse, cssCourse, typescriptCourse, cppCourse,
  javaCourse, sqlCourse, rCourse, gitCourse, 
  dockerCourse, aiPromptsCourse } from './otherCourses'

// ===== PYTHON COURSE =====
const pythonCourse = {
  id: 'python', title: 'Learn Python', icon: '🐍', language: 'python',
  description: 'A free, interactive Python course. Write code on every lesson, get hints when stuck.',
  estimatedHours: 15,
  lessons: [
    { id: 'py-01', title: 'Hello World', type: 'lesson', theory: `<h2>Your First Program</h2><p>The <code>print()</code> function displays text on the screen.</p><pre><code>print("Hello, World!")</code></pre><p>Every programmer starts here. Put your text inside quotes inside the parentheses.</p>`, challenge: 'Use print() to display "Hello, World!"', starterCode: '# Print Hello, World!\n', expectedOutput: 'Hello, World!', hint: 'print("Hello, World!")', quiz: { question: 'What function displays output in Python?', options: ['print()', 'echo()', 'display()', 'show()'], correctAnswer: 0 } },
    { id: 'py-02', title: 'Variables', type: 'lesson', theory: `<h2>Variables</h2><p>A variable stores a value you can use later.</p><pre><code>name = "Alice"\nage = 25\nprint(name)\nprint(age)</code></pre><p>No need to declare a type — Python figures it out.</p>`, challenge: 'Create a variable called city with value "Tokyo", then print it.', starterCode: '# Create and print a variable\n', expectedOutput: 'Tokyo', hint: 'city = "Tokyo" then print(city)', quiz: { question: 'How do you create a variable in Python?', options: ['name = value', 'var name = value', 'let name = value', 'dim name'], correctAnswer: 0 } },
    { id: 'py-03', title: 'Data Types', type: 'lesson', theory: `<h2>Data Types</h2><p>Python has several types:</p><ul><li><strong>int</strong> — whole numbers: 42</li><li><strong>float</strong> — decimals: 3.14</li><li><strong>str</strong> — text: "hello"</li><li><strong>bool</strong> — True or False</li></ul><pre><code>x = 42\nprint(type(x))  # &lt;class 'int'&gt;</code></pre>`, challenge: 'Create an int, a float, and a string. Print the type of each.', starterCode: '# Create three variables of different types\n', expectedOutput: "<class 'int'>", hint: 'Use type() around each variable', quiz: { question: 'What type is 3.14?', options: ['float', 'int', 'str', 'decimal'], correctAnswer: 0 } },
    { id: 'py-04', title: 'Strings', type: 'lesson', theory: `<h2>Strings</h2><p>Strings are text in quotes. Common operations:</p><pre><code>name = "Python"\nprint(len(name))      # 6\nprint(name.upper())   # PYTHON\nprint(name[0])        # P\nprint("Hello " + name)  # Hello Python</code></pre>`, challenge: 'Create a variable word = "python". Print its length and uppercase.', starterCode: '# String operations\n', expectedOutput: '6', hint: 'len() gives length, .upper() converts to uppercase', quiz: { question: 'What does len("code") return?', options: ['4', '3', '5', 'Error'], correctAnswer: 0 } },
    { id: 'py-05', title: 'Numbers & Math', type: 'lesson', theory: `<h2>Arithmetic</h2><pre><code>a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (integer division)\nprint(a % b)   # 1 (remainder)\nprint(a ** b)  # 1000 (power)</code></pre>`, challenge: 'Print 2 to the power of 8, and the remainder of 17 divided by 5.', starterCode: '# Math operations\n', expectedOutput: '256', hint: 'Use ** for power, % for remainder', quiz: { question: 'What does 10 // 3 return?', options: ['3', '3.33', '4', '1'], correctAnswer: 0 } },
    { id: 'py-06', title: 'If Statements', type: 'lesson', theory: `<h2>Conditionals</h2><pre><code>age = 18\nif age >= 18:\n    print("Adult")\nelif age >= 13:\n    print("Teenager")\nelse:\n    print("Child")</code></pre><p>Indentation (4 spaces) defines blocks. Use elif for multiple conditions.</p>`, challenge: 'If score is 85: print "A" if >= 90, "B" if >= 80, else "C".', starterCode: 'score = 85\n# Write your if/elif/else\n', expectedOutput: 'B', hint: 'if score >= 90: ... elif score >= 80: ... else: ...', quiz: { question: 'What keyword checks another condition after if?', options: ['elif', 'else if', 'elseif', 'otherwise'], correctAnswer: 0 } },
    { id: 'py-07', title: 'For Loops', type: 'lesson', theory: `<h2>For Loops</h2><pre><code>for i in range(5):\n    print(i)  # 0, 1, 2, 3, 4\n\nfor char in "Hello":\n    print(char)</code></pre><p><code>range(n)</code> gives 0 to n-1. <code>range(1, 6)</code> gives 1 to 5.</p>`, challenge: 'Print numbers 1 through 5 using a for loop.', starterCode: '# Print 1 to 5\n', expectedOutput: '1', hint: 'for i in range(1, 6): print(i)', quiz: { question: 'What does range(3) produce?', options: ['0, 1, 2', '1, 2, 3', '0, 1, 2, 3', '1, 2'], correctAnswer: 0 } },
    { id: 'py-08', title: 'While Loops', type: 'lesson', theory: `<h2>While Loops</h2><pre><code>count = 0\nwhile count < 5:\n    print(count)\n    count += 1</code></pre><p>Runs as long as the condition is True. Always update the variable or you get an infinite loop!</p>`, challenge: 'Print even numbers from 2 to 10 using a while loop.', starterCode: '# Even numbers 2-10\n', expectedOutput: '2', hint: 'Start at 2, loop while <= 10, add 2 each time', quiz: { question: 'When does a while loop stop?', options: ['When condition is False', 'After 10 iterations', 'When break is called', 'Never'], correctAnswer: 0 } },
    { id: 'py-09', title: 'Lists', type: 'lesson', theory: `<h2>Lists</h2><pre><code>fruits = ["apple", "banana", "cherry"]\nprint(fruits[0])        # apple\nfruits.append("date")   # add to end\nprint(len(fruits))      # 4</code></pre><p>Lists are ordered, mutable collections. Index starts at 0.</p>`, challenge: 'Create list [1,2,3], append 4, print the list and its length.', starterCode: '# List operations\n', expectedOutput: '[1, 2, 3, 4]', hint: '.append() adds, len() counts', quiz: { question: 'What index is the first element?', options: ['0', '1', '-1', 'first'], correctAnswer: 0 } },
    { id: 'py-10', title: 'Dictionaries', type: 'lesson', theory: `<h2>Dictionaries</h2><pre><code>person = {"name": "Alice", "age": 30}\nprint(person["name"])     # Alice\nperson["city"] = "Tokyo"  # add key\nprint(person.keys())</code></pre><p>Key-value pairs. Keys must be unique.</p>`, challenge: 'Create dict with "brand": "Toyota", "year": 2023. Print the brand.', starterCode: '# Dictionary\n', expectedOutput: 'Toyota', hint: 'dict["key"] to access values', quiz: { question: 'How do you access a dict value?', options: ['dict["key"]', 'dict.key', 'dict(key)', 'dict->key'], correctAnswer: 0 } },
    { id: 'py-11', title: 'Functions', type: 'lesson', theory: `<h2>Functions</h2><pre><code>def greet(name):\n    return f"Hello, {name}!"\n\nresult = greet("World")\nprint(result)  # Hello, World!</code></pre><p><code>def</code> defines a function. <code>return</code> sends a value back.</p>`, challenge: 'Create a function square(n) that returns n*n. Call with 7, print result.', starterCode: '# Define and call function\n', expectedOutput: '49', hint: 'def square(n): return n * n', quiz: { question: 'What keyword defines a function?', options: ['def', 'function', 'func', 'fn'], correctAnswer: 0 } },
    { id: 'py-12', title: 'List Comprehensions', type: 'lesson', theory: `<h2>List Comprehensions</h2><pre><code># Regular way\nsquares = []\nfor x in range(5):\n    squares.append(x**2)\n\n# Comprehension way\nsquares = [x**2 for x in range(5)]\nprint(squares)  # [0, 1, 4, 9, 16]</code></pre><p>Compact way to create lists. Can include conditions: <code>[x for x in range(10) if x % 2 == 0]</code></p>`, challenge: 'Create a list of squares for numbers 1-5 using a comprehension. Print it.', starterCode: '# List comprehension\n', expectedOutput: '[1, 4, 9, 16, 25]', hint: '[x**2 for x in range(1, 6)]', quiz: { question: 'What does [x*2 for x in [1,2,3]] produce?', options: ['[2, 4, 6]', '[1, 2, 3]', '[2, 2, 2]', 'Error'], correctAnswer: 0 } },
    { id: 'py-13', title: 'Tuples', type: 'lesson', theory: `<h2>Tuples</h2><pre><code>point = (3, 7)\nprint(point[0])  # 3\n\n# Tuples are IMMUTABLE (can't change)\n# point[0] = 5  # Error!\n\n# Unpacking\nx, y = point\nprint(x, y)  # 3 7</code></pre>`, challenge: 'Create a tuple colors = ("red", "green", "blue"). Print the second element.', starterCode: '# Tuple operations\n', expectedOutput: 'green', hint: 'Tuples use () and index with [1] for second element', quiz: { question: 'Can you modify a tuple after creation?', options: ['No', 'Yes', 'Only with .append()', 'Only numbers'], correctAnswer: 0 } },
    { id: 'py-14', title: 'String Methods', type: 'lesson', theory: `<h2>String Methods</h2><pre><code>text = "Hello, World!"\nprint(text.lower())       # hello, world!\nprint(text.replace("World", "Python"))\nprint(text.split(", "))   # ['Hello', 'World!']\nprint("  hi  ".strip())   # 'hi'</code></pre>`, challenge: 'Take "hello world" and print it with each word capitalized (title case).', starterCode: '# String methods\n', expectedOutput: 'Hello World', hint: 'Use .title() method', quiz: { question: 'What does .split(",") do?', options: ['Splits string into list at commas', 'Removes commas', 'Adds commas', 'Counts commas'], correctAnswer: 0 } },
    { id: 'py-15', title: 'File I/O', type: 'lesson', theory: `<h2>Reading and Writing Files</h2><pre><code># Write to file\nwith open("data.txt", "w") as f:\n    f.write("Hello, File!")\n\n# Read from file\nwith open("data.txt", "r") as f:\n    content = f.read()\n    print(content)</code></pre><p>The <code>with</code> statement auto-closes the file. Modes: "r" read, "w" write, "a" append.</p>`, challenge: 'Write code to write "Python is fun" to a file and read it back.', starterCode: '# File operations\n', expectedOutput: 'Python is fun', hint: 'Use with open("file.txt", "w") as f: f.write(...)', quiz: { question: 'What does "w" mode do?', options: ['Write (overwrites)', 'Watch', 'Wait', 'Write (appends)'], correctAnswer: 0 } },
    { id: 'py-16', title: 'Error Handling', type: 'lesson', theory: `<h2>Try / Except</h2><pre><code>try:\n    result = 10 / 0\nexcept ZeroDivisionError:\n    print("Can't divide by zero!")\nexcept Exception as e:\n    print(f"Error: {e}")\nfinally:\n    print("This always runs")</code></pre>`, challenge: 'Write a try/except that catches dividing by zero and prints "Error!"', starterCode: '# Error handling\n', expectedOutput: 'Error!', hint: 'try: 10/0 \\nexcept: print("Error!")', quiz: { question: 'What keyword catches an error?', options: ['except', 'catch', 'handle', 'rescue'], correctAnswer: 0 } },
    { id: 'py-17', title: 'Classes & Objects', type: 'lesson', theory: `<h2>Object-Oriented Programming</h2><pre><code>class Dog:\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n    \n    def bark(self):\n        return f"{self.name} says Woof!"\n\nmy_dog = Dog("Rex", 3)\nprint(my_dog.bark())</code></pre>`, challenge: 'Create a class Car with brand and year. Create an instance and print brand.', starterCode: '# Define a class\n', expectedOutput: 'Toyota', hint: 'class Car: def __init__(self, brand, year): self.brand = brand', quiz: { question: 'What method initializes a class?', options: ['__init__', '__start__', 'constructor', 'new'], correctAnswer: 0 } },
    { id: 'py-18', title: 'Lambda Functions', type: 'lesson', theory: `<h2>Lambda (Anonymous Functions)</h2><pre><code># Regular\ndef double(x):\n    return x * 2\n\n# Lambda equivalent\ndouble = lambda x: x * 2\n\n# Used with map/filter\nnums = [1, 2, 3, 4]\nsquared = list(map(lambda x: x**2, nums))\nprint(squared)  # [1, 4, 9, 16]</code></pre>`, challenge: 'Use a lambda with map to triple each number in [1,2,3,4,5]. Print result.', starterCode: '# Lambda with map\n', expectedOutput: '[3, 6, 9, 12, 15]', hint: 'list(map(lambda x: x*3, [1,2,3,4,5]))', quiz: { question: 'Lambda functions are...', options: ['Anonymous one-line functions', 'Named functions', 'Class methods', 'Error handlers'], correctAnswer: 0 } },
    { id: 'py-19', title: 'Modules & Imports', type: 'lesson', theory: `<h2>Importing Modules</h2><pre><code>import math\nprint(math.sqrt(16))  # 4.0\n\nfrom random import randint\nprint(randint(1, 10))\n\nimport os\nprint(os.getcwd())</code></pre><p>Python has thousands of built-in modules. Use <code>import</code> to use them.</p>`, challenge: 'Import math and print the value of pi and sqrt of 144.', starterCode: '# Import and use a module\n', expectedOutput: '3.14159', hint: 'import math; print(math.pi); print(math.sqrt(144))', quiz: { question: 'How do you import just one function?', options: ['from module import function', 'import function from module', 'include module.function', 'require(function)'], correctAnswer: 0 } },
    { id: 'py-20', title: 'Decorators', type: 'lesson', theory: `<h2>Decorators</h2><pre><code>def timer(func):\n    def wrapper(*args):\n        import time\n        start = time.time()\n        result = func(*args)\n        print(f"Took {time.time()-start:.4f}s")\n        return result\n    return wrapper\n\n@timer\ndef slow_add(a, b):\n    import time; time.sleep(0.1)\n    return a + b\n\nprint(slow_add(2, 3))</code></pre><p>Decorators wrap functions to add behavior. The @syntax is syntactic sugar.</p>`, challenge: 'Create a decorator that prints "Calling..." before a function runs.', starterCode: '# Create a decorator\n', expectedOutput: 'Calling...', hint: 'def my_decorator(func): def wrapper(): print("Calling..."); func(); return wrapper', quiz: { question: 'What symbol is used for decorators?', options: ['@', '#', '$', '&'], correctAnswer: 0 } },
  ],
}

// ===== JAVASCRIPT COURSE =====
const javascriptCourse = {
  id: 'javascript', title: 'Learn JavaScript', icon: '⚡', language: 'javascript',
  description: 'The language of the web. Build interactive pages and apps.',
  estimatedHours: 12,
  lessons: [
    { id: 'js-01', title: 'Hello World', type: 'lesson', theory: `<h2>JavaScript Basics</h2><p>Use <code>console.log()</code> to output text.</p><pre><code>console.log("Hello, World!");\nconsole.log(2 + 2);</code></pre><p>JS runs in browsers and Node.js. Semicolons are optional but recommended.</p>`, challenge: 'Print "Hello, JavaScript!" using console.log()', starterCode: '// Print your message\n', expectedOutput: 'Hello, JavaScript!', hint: 'console.log("Hello, JavaScript!")', quiz: { question: 'What outputs text in JS?', options: ['console.log()', 'print()', 'echo()', 'write()'], correctAnswer: 0 } },
    { id: 'js-02', title: 'Variables', type: 'lesson', theory: `<h2>Variables</h2><pre><code>let score = 100;    // can change\nconst name = "JS";  // cannot change\nvar old = "avoid";  // old way\n\nscore = 200;  // OK\n// name = "X";  // Error!</code></pre><p>Use <code>let</code> for values that change, <code>const</code> for constants.</p>`, challenge: 'Create let count = 5 and const lang = "JavaScript". Print both.', starterCode: '// Variables\n', expectedOutput: '5', hint: 'let count = 5; const lang = "JavaScript"; console.log(count); console.log(lang);', quiz: { question: 'Which cannot be reassigned?', options: ['const', 'let', 'var', 'all can'], correctAnswer: 0 } },
    { id: 'js-03', title: 'Data Types', type: 'lesson', theory: `<h2>Types</h2><pre><code>let text = "hello";   // string\nlet num = 42;         // number\nlet pi = 3.14;        // number (no separate float)\nlet flag = true;      // boolean\nlet empty = null;     // intentionally empty\nlet x;                // undefined\n\nconsole.log(typeof num);  // "number"</code></pre>`, challenge: 'Create a string, number, and boolean. Print typeof each.', starterCode: '// Types\n', expectedOutput: 'string', hint: 'typeof gives you the type as a string', quiz: { question: 'typeof 3.14 is?', options: ['number', 'float', 'decimal', 'double'], correctAnswer: 0 } },
    { id: 'js-04', title: 'Functions', type: 'lesson', theory: `<h2>Functions</h2><pre><code>// Declaration\nfunction add(a, b) {\n    return a + b;\n}\n\n// Arrow function\nconst multiply = (a, b) => a * b;\n\nconsole.log(add(2, 3));      // 5\nconsole.log(multiply(4, 5)); // 20</code></pre>`, challenge: 'Create function multiply(a,b) returning a*b. Call with 4,7 and print.', starterCode: '// Function\n', expectedOutput: '28', hint: 'function multiply(a,b) { return a*b; }', quiz: { question: 'Arrow function syntax?', options: ['(a,b) => a+b', 'a,b -> a+b', 'fn(a,b) = a+b', 'lambda a,b: a+b'], correctAnswer: 0 } },
    { id: 'js-05', title: 'Arrays', type: 'lesson', theory: `<h2>Arrays</h2><pre><code>const nums = [1, 2, 3, 4, 5];\nnums.push(6);           // add to end\nnums.pop();             // remove from end\nconsole.log(nums.length); // 5\n\nconst doubled = nums.map(n => n * 2);\nconst evens = nums.filter(n => n % 2 === 0);</code></pre>`, challenge: 'Create array [1,2,3], push 4, print array and length.', starterCode: '// Arrays\n', expectedOutput: '4', hint: '.push() adds, .length for count', quiz: { question: 'Add to end of array?', options: ['push()', 'append()', 'add()', 'insert()'], correctAnswer: 0 } },
    { id: 'js-06', title: 'Objects', type: 'lesson', theory: `<h2>Objects</h2><pre><code>const user = {\n    name: "Alice",\n    age: 25,\n    greet() {\n        return \`Hi, I'm \${this.name}\`;\n    }\n};\n\nconsole.log(user.name);    // Alice\nconsole.log(user.greet()); // Hi, I'm Alice</code></pre>`, challenge: 'Create object car with brand "Toyota", year 2024. Print brand.', starterCode: '// Object\n', expectedOutput: 'Toyota', hint: 'const car = { brand: "Toyota", year: 2024 };', quiz: { question: 'Access object property?', options: ['obj.key or obj["key"]', 'obj->key', 'obj(key)', 'obj::key'], correctAnswer: 0 } },
    { id: 'js-07', title: 'If/Else & Ternary', type: 'lesson', theory: `<h2>Conditionals</h2><pre><code>const age = 18;\n\nif (age >= 18) {\n    console.log("Adult");\n} else if (age >= 13) {\n    console.log("Teen");\n} else {\n    console.log("Child");\n}\n\n// Ternary (shortcut)\nconst status = age >= 18 ? "Adult" : "Minor";</code></pre>`, challenge: 'Use a ternary to print "even" if 10 is even, else "odd".', starterCode: '// Conditional\n', expectedOutput: 'even', hint: 'console.log(10 % 2 === 0 ? "even" : "odd")', quiz: { question: 'Ternary syntax?', options: ['condition ? a : b', 'if condition then a else b', 'condition ?? a :: b', 'condition | a | b'], correctAnswer: 0 } },
    { id: 'js-08', title: 'Loops', type: 'lesson', theory: `<h2>Loops</h2><pre><code>// For loop\nfor (let i = 0; i < 5; i++) {\n    console.log(i);\n}\n\n// For...of (arrays)\nconst fruits = ["apple", "banana"];\nfor (const fruit of fruits) {\n    console.log(fruit);\n}\n\n// While\nlet n = 3;\nwhile (n > 0) { console.log(n--); }</code></pre>`, challenge: 'Print numbers 1 to 5 using a for loop.', starterCode: '// Loop\n', expectedOutput: '1', hint: 'for (let i = 1; i <= 5; i++) console.log(i)', quiz: { question: 'for...of iterates over?', options: ['Values', 'Indices', 'Keys', 'Types'], correctAnswer: 0 } },
    { id: 'js-09', title: 'Destructuring', type: 'lesson', theory: `<h2>Destructuring</h2><pre><code>// Array destructuring\nconst [a, b, c] = [1, 2, 3];\n\n// Object destructuring\nconst { name, age } = { name: "Alice", age: 25 };\n\n// With renaming\nconst { name: userName } = { name: "Bob" };\nconsole.log(userName); // Bob</code></pre>`, challenge: 'Destructure {x: 10, y: 20} into variables x and y. Print x + y.', starterCode: '// Destructuring\n', expectedOutput: '30', hint: 'const {x, y} = {x: 10, y: 20};', quiz: { question: 'Destructure array [1,2,3] into a,b,c?', options: ['const [a,b,c] = arr', 'const {a,b,c} = arr', 'let a,b,c = arr', 'var [a,b,c] := arr'], correctAnswer: 0 } },
    { id: 'js-10', title: 'Promises & Async', type: 'lesson', theory: `<h2>Async JavaScript</h2><pre><code>// Promise\nconst wait = (ms) => new Promise(resolve => \n    setTimeout(resolve, ms)\n);\n\n// Async/Await\nasync function fetchData() {\n    await wait(1000);\n    return "Done!";\n}\n\nfetchData().then(console.log);</code></pre><p>async/await makes asynchronous code look synchronous.</p>`, challenge: 'Create an async function that returns "Hello Async" and log it.', starterCode: '// Async\n', expectedOutput: 'Hello Async', hint: 'async function f() { return "Hello Async"; } f().then(console.log)', quiz: { question: 'await can only be used inside?', options: ['async function', 'any function', 'class', 'module'], correctAnswer: 0 } },
  ],
}

// ===== COMBINED COURSES ARRAY =====
export const courses = [
  pythonCourse,
  javascriptCourse,
  htmlCourse,
  cssCourse,
  typescriptCourse,
  cppCourse,
  javaCourse,
  sqlCourse,
  rCourse,
  gitCourse,
  dockerCourse,
  aiPromptsCourse,
]

// ===== HELPER FUNCTIONS (used by pages) =====
export function getAllCourses() {
  return courses
}

export function getCourse(courseId) {
  return courses.find(c => c.id === courseId) || null
}

export function getLesson(courseId, lessonId) {
  const course = getCourse(courseId)
  if (!course) return null
  return course.lessons.find(l => l.id === lessonId) || null
}

export function getNextLesson(courseId, currentLessonId) {
  const course = getCourse(courseId)
  if (!course) return null
  const idx = course.lessons.findIndex(l => l.id === currentLessonId)
  return idx >= 0 && idx < course.lessons.length - 1 ? course.lessons[idx + 1] : null
}
